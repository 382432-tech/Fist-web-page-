# Running the project

This is a static HTML page. The Replit workflow serves the project directory on port 5000.

To run it manually:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Open the Replit preview to view `index.html`.